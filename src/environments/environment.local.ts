const BE_CONFIG = {
  BASE_URL: 'http://localhost:10001',
  PREFIX: '/api',
  VERSION: '/v1',
};

const MCE_CONFIG = {
  API_KEY: 'my-api-key',
};

export const environment = {
  production: false,
  BE_CONFIG,
  MCE_CONFIG,
};
