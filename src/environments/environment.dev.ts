const BE_CONFIG = {
  BASE_URL: 'https://erp-admin-portal-api-dev.ucars.sg',
  PREFIX: '/api',
  VERSION: '/v1',
};

const MCE_CONFIG = {
  API_KEY: 'my-api-key',
};

export const environment = {
  production: false,
  BE_CONFIG,
  MCE_CONFIG,
};
